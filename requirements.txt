# Runtime — the only third-party dependency; everything else is stdlib.
# (Deliberately no openai SDK: it would drop llama-server's non-standard
# `timings` block that the benchmark reads. See scripts/llm_bench/client.py.)
httpx>=0.27

# Tests
pytest>=8
